---
import BaseLayout from '../layouts/BaseLayout.astro';
import Card from '../components/ui/Card.astro';
import DividerList from '../components/ui/DividerList.astro';
import Button from '../components/ui/Button.astro';
import Callout from '../components/ui/Callout.astro';
import PageHeader from '../components/layout/PageHeader.astro';
import JsonLd from '../components/seo/JsonLd.astro';
import { buildHowTo } from '../lib/structuredData';
import { ArrowRight, BadgeCheck, Gift, ShoppingBag, ExternalLink, Info } from '@lucide/astro';
import { canonicalFor } from '../config/site';
import { minecraftGilde } from '../config/minecraftGilde';

const voteSites = [
  {
    name: 'minecraft-server.eu',
    href: minecraftGilde.voting.minecraftServerEuVoteUrl,
    desc: 'Vote auf minecraft-server.eu und sichere dir Belohnungen ingame.',
    active: true,
  },
  {
    name: 'minecraft-serverlist.net',
    href: minecraftGilde.voting.minecraftServerlistNetVoteUrl,
    desc: 'Vote auf minecraft-serverlist.net und sammle Vote-Punkte.',
    active: true,
  },
  {
    name: 'serverliste.net',
    href: minecraftGilde.voting.serverlisteNetVoteUrl,
    desc: 'Vote auf serverliste.net - jeder Vote hilft beim Ranking.',
    active: true,
  },
  {
    name: 'findmcserver.com',
    href: '#',
    desc: 'Hier könnt ihr bald auf der offiziellen Minecraft-Serverliste voten.',
    active: false,
  },
];

const canonicalUrl = canonicalFor(Astro.url.pathname);
const howToLd = buildHowTo({
  canonicalUrl,
  name: 'Für Minecraft Gilde voten',
  description:
    'So votest du auf Serverlisten, verbesserst das Ranking und erhältst Vote-Belohnungen sowie Vote-Punkte für den Vote-Shop.',
  steps: [
    {
      name: 'Vote-Link öffnen',
      text: 'Öffne eine der Serverlisten und gehe zur Vote-Seite der Minecraft Gilde.',
    },
    {
      name: 'Vote abschließen',
      text: 'Bestätige den Vote auf der jeweiligen Seite (ggf. mit Captcha) und warte, bis die Bestätigung angezeigt wird.',
    },
    {
      name: 'Optional: auf mehreren Listen voten',
      text: 'Wenn du mehrere Listen nutzt, sammelst du schneller Vote-Punkte für den Vote-Shop.',
    },
    {
      name: 'Belohnung ingame erhalten',
      text: 'Komm danach ingame - die Vote-Belohnung wird automatisch gutgeschrieben (zufällig) und Vote-Punkte werden gesammelt.',
    },
  ],
});
---

<BaseLayout
  title="Minecraft Gilde - Voten"
  canonical={canonicalUrl}
  description="Voten für Minecraft Gilde: Unterstütze den Server, verbessere das Ranking und erhalte Belohnungen sowie Vote-Punkte für den Vote-Shop."
>
  <JsonLd slot="head" data={howToLd} />
  <PageHeader
    title="Voten"
    subtitle="Mit jedem Vote machst du die Minecraft Gilde sichtbarer. Das hilft uns beim Ranking und sorgt dafür, dass neue Spieler leichter zu uns finden."
    class="pb-8"
    subtitleClass="text-muted mt-2 max-w-3xl"
  >
    <div class="mt-6 grid gap-4 lg:grid-cols-3" data-motion-stagger="70">
      <Card class="p-6" reveal>
        <div class="flex items-start gap-4">
          <div class="border-border/70 bg-accent/10 text-accent mt-0.5 flex h-12 w-12 shrink-0 items-center justify-center rounded-xl border">
            <Gift class="h-7 w-7" />
          </div>
          <div>
            <h2 class="text-base font-semibold">Belohnungen</h2>
            <p class="text-muted mt-2 text-sm leading-relaxed">
              Jeder Vote bringt dir eine zufällige Belohnung, zum Beispiel Ressourcen,
              Verbrauchsitems oder nützliche Ausrüstung.
            </p>
          </div>
        </div>
      </Card>

      <Card class="p-6" reveal>
        <div class="flex items-start gap-4">
          <div class="border-border/70 bg-accent/10 text-accent mt-0.5 flex h-12 w-12 shrink-0 items-center justify-center rounded-xl border">
            <ShoppingBag class="h-7 w-7" />
          </div>
          <div>
            <h2 class="text-base font-semibold">Vote-Punkte</h2>
            <p class="text-muted mt-2 text-sm leading-relaxed">
              Pro Vote erhältst du 1 Vote-Punkt (maximal 3 pro Tag), die du im Vote-Shop gegen
              exklusive Items eintauschen kannst.
            </p>
          </div>
        </div>
      </Card>

      <Card class="p-6" reveal>
        <div class="flex items-start gap-4">
          <div class="border-border/70 bg-accent/10 text-accent mt-0.5 flex h-12 w-12 shrink-0 items-center justify-center rounded-xl border">
            <BadgeCheck class="h-7 w-7" />
          </div>
          <div>
            <h2 class="text-base font-semibold">Ranking</h2>
            <p class="text-muted mt-2 text-sm leading-relaxed">
              Regelmäßiges Voting verbessert unseren Rang und hält die Community langfristig aktiv.
            </p>
          </div>
        </div>
      </Card>
    </div>
  </PageHeader>

  <section class="mg-container pb-16">
    <div class="flex items-end justify-between gap-6">
      <div>
        <h2 class="text-xl font-semibold tracking-tight">Vote-Links</h2>
        <p class="text-muted mt-2 max-w-3xl text-sm">
          Wähle einfach eine Liste aus, vote kurz und komm danach zurück ins Spiel - die Belohnung
          wird automatisch gutgeschrieben.
        </p>
      </div>
      <div class="hidden sm:block">
        <Button href="/faq" variant="ghost" size="sm" class="gap-2">
          Häufige Fragen <ArrowRight size={16} />
        </Button>
      </div>
    </div>

    <div class="mg-card mg-card--soft mt-6 p-2 sm:p-3" data-motion="reveal">
      <DividerList as="div" class="mg-list--straight">
        {voteSites.map((s) =>
          s.active ? (
            <div class="mg-row flex items-start justify-between gap-4 px-2 py-4 sm:px-3">
              <div class="min-w-0">
                <h3 class="text-base font-semibold">{s.name}</h3>
                <p class="text-muted mt-1.5 text-sm leading-relaxed">{s.desc}</p>
              </div>
              <Button href={s.href} external variant="secondary" size="sm" class="shrink-0 gap-1.5">
                Vote <ExternalLink size={14} />
              </Button>
            </div>
          ) : (
            <div class="mg-row flex items-start justify-between gap-4 px-2 py-4 sm:px-3">
              <div class="min-w-0">
                <div class="flex items-center gap-2">
                  <h3 class="text-base font-semibold">{s.name}</h3>
                  <span class="bg-surface border-border text-muted inline-flex items-center rounded-full border px-2 py-0.5 text-xs font-medium">
                    Demnächst
                  </span>
                </div>
                <p class="text-muted mt-1.5 text-sm leading-relaxed">{s.desc}</p>
              </div>
              <Button disabled variant="secondary" size="sm" class="shrink-0 gap-1.5">
                Bald <Info size={14} />
              </Button>
            </div>
          ),
        )}
      </DividerList>
    </div>

    <Callout variant="info" title="Wichtige Hinweise" class="mt-6" data-motion="reveal">
      <ul class="list-disc space-y-1.5 pl-5">
        <li>
          Du kannst auch außerhalb des Spiels voten. Die Belohnung wird beim nächsten Login
          automatisch gutgeschrieben.
        </li>
        <li>
          Achte auf deinen exakten Minecraft-Namen, sonst kann dein Vote nicht korrekt zugeordnet
          werden.
        </li>
        <li>Du erhältst 1 Vote-Punkt pro Vote, maximal 3 Vote-Punkte pro Tag.</li>
      </ul>
      <p class="mt-3">
        Voting ist freiwillig, hilft aber direkt beim Server-Ranking und sorgt für eine aktivere
        Community.
      </p>
    </Callout>
  </section>
</BaseLayout>
